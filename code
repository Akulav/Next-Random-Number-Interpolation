#include <stdio.h>
int n,i,num;
void main()
{
    FILE *f;
    f = fopen("primein.txt","r");
    fscanf(f,"%d",&num);
    fclose (f);
    for(i=1;i<=num;i++)
        {
   if(num%i==0)
   {
    printf("\n%d",i);
   }
  }
}
